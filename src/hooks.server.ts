import { buildCreatureIndex } from "$lib/search/creatures";
import { buildSpellIndex } from "$lib/search/spells";
import { buildSiteIndex } from "$lib/search/site";

buildCreatureIndex();
buildSpellIndex();
buildSiteIndex();
